import React from "react";

const Brand = ({brand}) => {
  return (

      <div className="mt-16 flex flex-wrap justify-center items-center gap-12 px-10 pt-30">
        <span className="text-[#D9EFDE] text-xl md:text-2xl font-medium">{brand}</span>

    
      </div>

  );
};

export default Brand;
